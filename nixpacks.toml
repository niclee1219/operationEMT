[phases.setup]
nixPkgs = ["python311", "nodejs_20", "nodePackages.npm"]

[phases.build]
cmds = [
  "cd frontend/dashboard && npm ci && npm run build && cd ../.."
]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
